// ★★★★　以下課題　★★★★★
// 1.命名規則に従う理由
// 変数などを適当な名前にしてしまうとコードの可読性が下がる。
// 複数人で開発する場合、各々が自由に変数などの名前を付けてしまうと統一性がない。
// 可読性や統一性を持たせるために命名規則を用いる。

// 2.ケバブケースを使用したclass名
// class = test-ABC;

// 3.スネークケースを使用したclass名
// class = test_ABC;

// 4.キャメルケースを使用した変数名
let testNum = 10;
testNum = testNum * 2;
console.log(testNum);